import React from 'react';
import ReactDOM from 'react-dom';
import MeleeMessenger from './MeleeMessenger';

ReactDOM.render(
  <React.StrictMode>
    <MeleeMessenger />
  </React.StrictMode>,
  document.getElementById('root')
);
